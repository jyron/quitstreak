VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
VITE_STRIPE_PUBLISHABLE_KEY=pk_live_xxx
STRIPE_SECRET_KEY=sk_live_xxx
STRIPE_WEBHOOK_SECRET=whsec_xxx
VITE_APP_URL=https://quitstreak.app

# Dev-only: password auth bypass for testing onboarding locally (never set in production/CI)
# Setup: Supabase dashboard → Auth → Providers → enable Email/Password
#        Auth → Users → Add user → test@dev.local / devpassword123 → confirm email
VITE_DEV_EMAIL=test@dev.local
VITE_DEV_PASSWORD=devpassword123
